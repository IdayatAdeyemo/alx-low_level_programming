#include "main.h"

/**
 * _abs - check main
 * @r: an integre input
 * description: this function return absolute value of number
 * Return: absolute value of number 1
 *
 */

int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (r * -1);
}
