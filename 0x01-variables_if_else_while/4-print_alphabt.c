#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
*main - entry point
*
*Return: value 0 (success)
*
*/

int main(void)
{
	char sp = ' ';
	char low;


	while (low = 'a'; 'a' <= 'z'; low++)
	{
		if ((low = 'e') || (low = 'q'))
		{
			putchar(sp);
		}
		else
		{
			putchar(low);
		}
		putchar('\n');
	}
	return (0);
}

