#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
*main - Entry point
*
*Return: value 0 (success)
*
*/

int main(void)
{
	int i, j, k;

	for (i = 0; i <= 9; i++)
	{
		for (j = j + 1; j <= 9; j++)
		{
			for (k = k + 1; k <= 9; k++)
			{
				putchar(ijk);
			}
		}
	}
}
putchar('\n');
return (0);
